// External Dependencies
import React from 'react';

// Custom Dependencies
import WorldTimeWidget from '../components/WorldTimeWidget/WorldTimeWidget';

export default {
    title: 'World Time Widget',
    component: WorldTimeWidget,
};

// Empty World Time Widget


// 1 Entry in World Time Widget
export const OneEntryWorldTimeWidget = (args) => (
    <WorldTimeWidget />
);

// 3 Entries in World Time Widget
